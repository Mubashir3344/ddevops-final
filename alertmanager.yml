global:
  resolve_timeout: 5m

route:
  receiver: jenkins-rollback-webhook
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: jenkins-rollback-webhook
    webhook_configs:
      - url: 'http://3.95.191.245:8080/generic-webhook-trigger/invoke?token=ROLLBACK_A87B89_TOKEN'
        http_config:
          basic_auth:
            username: 'admin'
            password: '11cf26b29d4612a0629b18340d1ca50d7d'
        send_resolved: false
